name: test-cloud-functions
description: test-cloud-functions action for the pipeline

runs:
  using: "composite"
  steps:
    - name: Excel export is not configured
      shell: sh
      run: echo "Excel export is not configured"
